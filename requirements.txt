# =============================================================================
# requirements.txt
# Active runtime dependencies for the root Django project.
# Optional OCR / NLP packages live in requirements-optional.txt so the base
# install stays compatible with the current Python 3.14 toolchain.
# =============================================================================

# Core framework
Django==5.2.12
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.2

# Database
psycopg2-binary==2.9.11

# Background jobs
celery==5.6.2
redis==7.3.0
django-celery-beat==2.9.0

# Files / reporting
Pillow==12.1.1
openpyxl==3.1.5
reportlab==4.4.10
qrcode==8.2

# Runtime utilities
requests==2.32.5
bcrypt==5.0.0
python-decouple==3.8
gunicorn==25.1.0
whitenoise==6.12.0
django-ratelimit==4.1.0
