# Vite environment variables
# Copy this to .env.local and adjust for your LAN setup

# Backend API base URL (used in production build, dev uses Vite proxy to http://127.0.0.1:8000)
VITE_API_BASE_URL=http://192.168.1.100:8000/api/v1
