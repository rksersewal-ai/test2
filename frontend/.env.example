# =============================================================================
# FILE: frontend/.env.example
# Copy to .env.local and set your LAN backend URL
# =============================================================================

# Development (Vite dev server proxies /api -> backend)
VITE_API_BASE_URL=/api/v1

# LAN Production (point directly at backend host)
# VITE_API_BASE_URL=http://192.168.1.100:8000/api/v1
