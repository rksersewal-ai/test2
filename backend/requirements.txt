# =============================================================================
# EDMS-LDO backend requirements
# Install: pip install -r requirements.txt
# =============================================================================

# Django core
Django==5.2.12
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==25.2

# Database
psycopg2-binary==2.9.11       # PostgreSQL driver

# PDF / OCR processing
Pillow==10.3.0
pytesseract==0.3.10
pdfplumber==0.11.0
openpyxl==3.1.5
reportlab==4.4.10

# Async / task queue (optional — for Celery-based OCR)
celery==5.6.2
redis==7.3.0                  # Celery broker (local Redis on LAN)

# Utilities
python-dotenv==1.0.1          # .env file loading
gunicorn==25.1.0              # Production WSGI server
whitenoise==6.12.0            # Static file serving without separate web server
